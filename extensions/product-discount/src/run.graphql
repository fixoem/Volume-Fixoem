query RunInput {
  cart {
    lines {
      id
      quantity
    }
  }
  discountNode {
    metafield(namespace: "$app:promobooster-discount", key: "function-configuration") {
      value
    }
  }
}
